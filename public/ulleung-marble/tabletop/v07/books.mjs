import {characterCourse} from './character-courses.mjs';
import {esc,coverHTML,shipSVG,portraitStyle} from './components.mjs';
import {art} from './illustration-map.mjs';
export const turnHTML=`<ol class="steps"><li><b>이동 방법 선택</b><span>걷기 또는 택시를 고릅니다. 택시는 예산 1만원을 내고 해안길로만 갑니다.</span></li><li><b>주사위와 이동</b><span>걷기는 1개, 택시는 2개를 굴립니다. 나온 수 이하에서 1칸 이상 이동합니다.</span></li><li><b>같은 색 카드 1장</b><span>도착한 칸과 같은 색의 권역 조우 카드를 뽑습니다.</span></li><li><b>A/B 선택과 처리</b><span>상황과 두 선택의 비용·효과를 모두 읽고 하나를 고릅니다. 예산 큐브와 카드를 바꿉니다.</span></li><li><b>차례 넘기기</b><span>조우 카드를 버립니다. 모두 한 번씩 차례를 마치면 라운드 배를 한 칸 옮깁니다.</span></li></ol>`;
export const scoreHTML=`<table class="score-table"><thead><tr><th>항목</th><th>점수</th></tr></thead><tbody>${[['풍경','1장마다 1점'],['체험 / 미식','2장 / 3장마다 1점'],['여행자 목표','달성한 목표마다 3점'],['코스 도착 1 / 2 / 3곳','2 / 4 / 8점'],['달인 · 단독 최다','풍경 3 · 체험 5 · 미식 8점'],['달인 · 공동 최다','풍경 2 · 체험 3 · 미식 4점'],['일주','1장마다 7점'],['남은 예산','5만원마다 1점'],['피로 0 / 1 / 2 / 3 / 4장','0 / −1 / −3 / −6 / −10점'],['항구에서 종료','마지막 위치가 사동·도동·저동이면 3점']].map(r=>`<tr><td>${r[0]}</td><td>${r[1]}</td></tr>`).join('')}</tbody></table>`;
function page(title,body,no,label='여행자북',extra=''){return `<article class="b6-page ${label==='규칙서'?'rule-page':''} ${extra}"><header><b>울릉마블</b><span>${label}</span></header><h2>${title}</h2><div class="page-body">${body}</div><footer><span>ULLEUNG MARBLE</span><b>${no} / 8</b></footer></article>`;}
const para=t=>t.split('\n\n').map(s=>`<p>${esc(s)}</p>`).join('');
const artImage=(key,cls='book-illustration')=>`<img class="${cls}" src="${art(key)}" alt="울릉도 여행 삽화">`;
const callout=(title,body)=>`<aside><b>${title}</b><p>${body}</p></aside>`;
const boats=()=>`<div class="arrival-print">${[['cruise','크루즈','사동항','피로 0장','주사위 1개'],['fast','쾌속선','도동항','피로 1장','첫 이동만 2개']].map(([type,title,port,fatigue,dice])=>`<section>${shipSVG(type)}<h3>${title}</h3><b>${port}에서 시작</b><p>${fatigue}<br>${dice}</p></section>`).join('')}</div>`;
function courseCheck(d,c,courseId){const course=characterCourse(d,c,courseId);return `<div class="character-course"><div class="personal-course-title"><b>${esc(course.route.name)}</b><span>${course.route.id}</span></div><p class="course-motive">${esc(course.intro)}</p><div class="personal-stops">${course.stops.map(({node,reason},i)=>`<section><div><span>0${i+1}</span><h3>${esc(node.name)}</h3><b>□ 도착</b></div>${reason?`<p>${esc(reason)}</p>`:''}</section>`).join('')}</div><p class="course-visit-rule">순서는 자유입니다. 그 칸에서 이동을 마치면 표시합니다. 출발·통과는 세지 않습니다.</p><div class="course-points"><span>1곳 <b>2점</b></span><span>2곳 <b>4점</b></span><span>3곳 <b>8점</b></span></div><h3>일주 준비 · 다섯 권역</h3><div class="region-checks">${d.regions.map(r=>`<span><i style="background:${r.color}"></i>□ ${r.title}</span>`).join('')}</div><aside>다섯 권역에서 이동을 마친 뒤 사동·도동·저동항 중 한 곳에 도착하면 일주 1장(7점)을 받습니다. 기록은 유지하며, 1인 1회·먼저 달성한 4명까지 받습니다.</aside></div>`;}
export function bookPages(d,c,{courseId=null}={}){return [coverHTML(c),page('여행을 준비하며',para(c.stories[0]),2,'여행자북','story-page'),page('울릉도를 고른 이유',para(c.stories[1]),3,'여행자북','story-page'),page('배를 고르고 출발',`<p>${esc(c.stories[2])}</p>${boats()}${callout('공통 준비','예산 큐브는 50에 놓습니다. 여행자북 6쪽에 적힌 번호의 코스 카드를 찾아 공개합니다. 배에 적힌 항구에 미플을 놓습니다.')}`,4),page('이번 여행의 목표',c.goals.map((g,i)=>`<section class="goal"><small>목표 0${i+1} <strong>3점</strong></small><h3>${esc(g.title)}</h3><p>${esc(g.text)}</p></section>`).join('')+artImage(['gwanum','seongin','nari','food-mussel-rice','port','lighthouse','nari','coast'][c.id],'goal-art')+callout('게임이 끝나면','달성한 목표마다 3점을 받습니다. 코스 점수와 여행자 목표 점수는 각각 계산합니다.'),5),page('나의 여행코스',courseCheck(d,c,courseId),6,'여행자북','character-course-page'),page('내 차례',turnHTML+callout('카드에 적힌 선택만 처리','칸에 도착했다고 풍경·체험·미식을 바로 받지 않습니다. 조우 카드의 A/B 결과로 받습니다.'),7),page('여행의 점수',scoreHTML+`<p>7라운드의 마지막 사람까지 이동·카드 처리를 마치면 점수를 계산합니다. 합계가 같으면 공동 승리입니다.</p><aside>예산이 0이어도 걷기와 비용 없는 선택을 하며 계속 진행합니다. 항구 점수는 마지막 위치로 한 번만 받습니다.</aside>`,8)];}
export {manualPages} from './manual.mjs';
