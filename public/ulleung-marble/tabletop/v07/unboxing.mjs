import {modelImage} from './miniatures.mjs?v=20260923j';
import {COLORS,MOTIFS} from './models.mjs?v=20260923j';
export function mountUnboxing(host,{explore,prepare}){
 host.innerHTML=`<div class="unbox-intro"><span class="unbox-kicker">울릉마블</span><h1>상자 속 여행을 꺼내보세요</h1></div><div class="box-stage"><div class="package-shadow"></div><div class="package-base"><div class="box-liner"><div class="pack-board"><img src="/ulleung-marble/assets/v07/board.svg?v=20260923j" alt="접힌 지도 보드"></div><div class="pack-books"><span>여행자북 8권</span><img src="/ulleung-marble/assets/v07/book-cover-0.svg" alt="정하린 여행자북 표지"><small>여행자북 8권 · 규칙서 1권</small></div><div class="pack-cards">${['scenery','activity','food','fatigue','lap'].map(t=>`<img src="/ulleung-marble/assets/v07/cards/${t}-1-back.svg" alt="기록 카드 그림">`).join('')}</div><div class="pack-pieces">${Array.from({length:8},(_,i)=>`<img src="${modelImage('meeple',i)}" alt="${MOTIFS[i]} 미플">`).join('')}</div><div class="pack-cubes">${Array.from({length:8},(_,i)=>`<img src="${modelImage('marker',i)}" alt="${i+1}번 예산 큐브">`).join('')}</div><div class="pack-dice"><img src="${modelImage('die',0)}" alt="바다 주사위"><img src="${modelImage('die',1)}" alt="호박 주사위"></div></div></div><button class="package-lid" aria-label="울릉마블 상자 열기"><div class="lid-art"></div><div class="lid-type"><small>ULLEUNG MARBLE</small><strong>울릉마블</strong><span>울릉도 · 독도 여행 보드게임</span></div></button></div><div class="unbox-after" hidden><p>보드 1개 · 카드 233장 · 여행자북 8권 · 규칙서 1권<br>미플 8개 · 예산 큐브 8개 · 주사위 2개 · 라운드 배 · 연필</p><div><button id="explore-box">구성품 살펴보기</button><button class="primary" id="prepare-box">여행 준비</button></div></div><div class="unbox-caption"><button class="primary" id="open-box">상자 열기 <span aria-hidden="true">↗</span></button></div>`;
 const openBox=()=>{host.classList.add('opened');host.querySelector('.package-lid').disabled=true;host.querySelector('.unbox-caption').hidden=true;host.querySelector('.unbox-after').hidden=false;};
 host.querySelector('.package-lid').onclick=openBox;host.querySelector('#open-box').onclick=openBox;
 const leave=fn=>{host.hidden=true;document.querySelector('#play').inert=false;sessionStorage.setItem('ulleung-box-opened-v07','1');fn();};host.querySelector('#explore-box').onclick=()=>leave(explore);host.querySelector('#prepare-box').onclick=()=>leave(prepare);
}
