# 2026-09-27 배포 복구

## 원인

9월 26일 23:24 운영 배포는 로컬 `f531913`과 미커밋 변경을 사용한 CLI 배포였다. 이 이력에는 GitHub `main`의 활동·강의자료·게임 갱신·문구 수정이 합쳐져 있지 않았다. 기존 운영 화면 대신 일부 과거 화면이 노출됐다.

## 병합 기준

- 로컬 `f531913`과 원격 `036ef7e`를 병합했다. 복구 전 로컬 커밋은 `codex/pre-recovery-20260927`에 보존했다.
- 9월 19일 활동, 9월 16일 온라인 강의, 모두의 창업 자료, 영어 미스터리 리메이크, 잔향 탐정 0.79.0을 복원했다.
- 공개 중단한 퇴원일·유리 너머의 목소리의 페이지와 자료는 원격의 공개 중단 상태를 유지했다. 원본은 archive와 Git 이력에서 복구할 수 있다.
- 스피킹 기능·학습 기록 처리·비공개 음성 자료, 벽꿍 게임, 소개 영상, 작품 이미지와 운영 조건의 로컬 변경은 보존했다.
- 이전 수업 예고편은 리메이크와 설정이 달라 에피소드 하단에서 이전 버전 영상으로 구분했다. 영상 파일은 변경하지 않았다.
- 홈 수업 자료 이미지의 두 열 배치를 복원했다. 모바일에서 이미지가 한 장씩 크게 쌓이지 않도록 표시 크기를 확인했다.
- 기존 미커밋 변경인 macOS 전용 workerd 직접 의존성 제거를 보존했다. 비밀 값은 커밋하지 않았다.

## 재발 방지

`npm run deploy:check`는 원격 main과 현재 HEAD를 직접 비교하고 미커밋·미추적 파일을 검사한다. `npm run deploy:production`은 검사가 통과해야 CLI 배포를 실행한다. 이 검사는 직접 실행한 다른 명령을 시스템 차원에서 막지는 않는다. AGENTS.md에 동일한 배포 절차를 명시했다.

## 검증

- Next.js 빌드·TypeScript 검사 통과. middleware 명칭 전환 경고는 남아 있다.
- 공개 경로 47개를 지원 언어에 맞춰 확인하고 자동 테스트 144개를 통과했다.
- 잔향 탐정은 5개 화면 크기에서 실행·설정 저장·이어하기를 검사했다.
- 삭제 경로를 참조하던 개발 캐시만 `/private/tmp/dullg-next-cache.IqL84M`으로 옮겼다. 사용자 소스는 삭제하지 않았다.
- 실제 Google 계정으로 로그인하거나 사용자 Firestore 데이터를 변경하는 검사는 하지 않았다.
